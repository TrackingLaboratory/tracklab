defaults:
  - default

_target_: tracklab.wrappers.SportsMOT

name: "sportsmot"
nickname: "sm"
dataset_path: ${data_dir}/SportsMOT
eval_set: "val"

track_eval:
  dataset_class: MotChallenge2DBox
  BENCHMARK: "SportsMOT"
